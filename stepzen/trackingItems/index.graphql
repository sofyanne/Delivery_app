type Items {
  item_id: Int
  name: String
  price: Float
  quantity: Int
}
type TrackingItems {
  customer_id: String
  items: [Items]
}

type Query {
  getTrackingItemById(id: ID!): TrackingItems
    @rest(
      endpoint: "https://deliveryapp-e78a3-default-rtdb.europe-west1.firebasedatabase.app/trackingItems/$id.json"
    )
}
